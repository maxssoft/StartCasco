<resources  xmlns:tools="http://schemas.android.com/tools">

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>

        <item name="android:windowContentTransitions" tools:targetApi="lollipop">true</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="AppTheme.Transparent" parent="AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="SwitchTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlActivated">@color/colorPrimary</item>
        <item name="colorSwitchThumbNormal">@color/background</item>
        <item name="android:colorForeground">@color/text_secondary</item>
    </style>

    <style name="RadioTheme" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/colorPrimary</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
    </style>

    <style name="AppDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:background">@color/background</item>
    </style>

    <style name="Divider">
        <item name="android:background">@color/divider</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
    </style>

    <style name="TitleButton" parent="textButton"> <!-- Синяя кнопка с текстом-->
        <item name="android:layout_height">@dimen/button_size</item>
        <item name="android:background">@drawable/ripple_button</item>
        <item name="android:paddingLeft">@dimen/standard_margin</item>
        <item name="android:paddingRight">@dimen/standard_margin</item>
        <item name="android:textColor">@color/text_primary_w</item>
<!--        <item name="android:stateListAnimator" tools:ignore="NewApi">@null</item>
-->
    </style>

    <style name="TitleLightButton" parent="TitleButton"> <!-- Кнопка с синей рамкой и текстом -->
        <item name="android:background">@drawable/selector_light_button_background</item>
        <item name="android:textColor">@drawable/button_light_text_selector</item>
    </style>

</resources>
